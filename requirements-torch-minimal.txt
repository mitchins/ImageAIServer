# Minimal PyTorch backend for SmolVLM-256M and Granite Vision
# Focused on essential dependencies only - no complex quantization libs

# Core PyTorch
torch>=2.0.0
torchvision>=0.15.0  # For vision processing

# Transformers ecosystem (required for model loading)
transformers>=4.35.0
accelerate>=0.24.0

# Diffusion pipelines
diffusers>=0.17.1
safetensors>=0.3.1

# Essential tokenization
sentencepiece>=0.1.99
protobuf>=3.20.0

# Image handling
Pillow>=9.0.0

# Note: Both SmolVLM-256M and Granite Vision have GGUF versions available:
# - ggml-org/SmolVLM-256M-Instruct-GGUF 
# - lmstudio-community/granite-vision-3.2-2b-GGUF
# Consider using GGUF + llama.cpp for production deployment